---
export interface HomePagePostProps {
  isImgLeft?: boolean;
  isImgRight?: boolean;
  text: string;
  imageSrc: string;
  title?: string;
}
const {
  isImgLeft = false,
  isImgRight = false,
  text = "",
  imageSrc,
  title,
} = Astro.props as HomePagePostProps;
---

<div class="homePagePost w-full h-fit p-2 flex justify-center">
  <div
    class={`homePagePost__container w-[80%] h-full flex ${
      isImgLeft ? "flex-col-reverse" : "flex-col"
    } lg:flex-row gap-4 sm:gap-6 lg:gap-20 justify-center items-center`}
  >
    {
      isImgLeft ? (
        <img src={imageSrc} alt="Post image" class="homePagePost__image" />
      ) : (
        ""
      )
    }
    <div class="homePagePost__textBlock flex w-full flex-col gap-4">
      {
        title ? (
          <h4
            class={`homePagePost__title font-bold text-[1.6rem] sm:text-[2.5rem] md:text-[3rem] lg:text-[4rem] stylishText ${
              isImgLeft ? "text-left" : "text-right"
            }`}
          >
            {title}
          </h4>
        ) : (
          ""
        )
      }
      <p
        class={`homePagePost__text text-[1rem] sm:text-[1.3rem] md:text-[1.7rem] lg:text-[2.3rem] ${
          isImgLeft ? "text-left" : "text-right"
        }`}
      >
        {text}
      </p>
    </div>
    {
      isImgRight ? (
        <img src={imageSrc} alt="Post image" class="homePagePost__image" />
      ) : (
        ""
      )
    }
  </div>
</div>

<style lang="css">
  .homePagePost__image {
    @apply w-fit h-auto object-cover sm:w-[500px];
  }
</style>
